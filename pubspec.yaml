name: hidden_logo
description: Package that allows you to display hidden logo behind hardware barriers for iPhones
version: 0.0.9
repository: https://github.com/AndreySosnovyy/hidden_logo
issue_tracker: https://github.com/AndreySosnovyy/hidden_logo/issues
topics: [ widget, ui, logo, brand, dynamic-island ]

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  device_info_plus: ^11.1.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
